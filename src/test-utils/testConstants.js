export const LINEAR_SERIES = Array.from({ length: 20 }, (_, index) => index);
export const SMALL_SAMPLE_SIZE = 120;
export const DEFAULT_KAPLAN_MEIER_DATA = [1, 1, 2, 3];

export const LOESS_BANDWIDTH = 0.4;
export const STRICT_LINEAR_TOLERANCE = 1e-6;
export const RUNNING_QUANTILE_WINDOW = 11;

export const STL_TREND_MEAN_ABS_ERROR_LIMIT = 0.25;
export const STL_SEASONAL_PATTERN_DIFF_LIMIT = 0.3;
export const STL_RESIDUAL_MEAN_ABS_LIMIT = 0.6;

export const DEFAULT_CHANGE_POINT_WINDOW = 8;

export const DEFAULT_APNEA_CLUSTER_GAP_SEC = 120;
